Скачать файлы в одну директорию.\n
Запустить командную строку в данной директории
В командной строке написать pip install -r requirements.txt
В командной строке написать python bookkeeper\presenter.py
